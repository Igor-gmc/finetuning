# 🚀 Быстрый старт

## Запуск проекта

```bash
python main.py
```

## Что делает скрипт:

1. ✅ Создает RAG базу данных (Qdrant + SentenceTransformers)
2. ✅ Дообучает модель Qwen2.5-1.5B-Instruct с LoRA (на 1000 примерах)
3. ✅ Генерирует ответы на 5 тестовых вопросов обоими методами
4. ✅ Анализирует результаты с помощью GPT-4o-mini

## Результат:

Файл **`result.txt`** с:
- Ответами RAG системы
- Ответами Fine-tuned модели
- Детальным анализом качества от GPT-4o-mini

## ⏱️ Время выполнения:

- RAG индексация: ~5-10 минут
- Fine-tuning: ~30-60 минут (на 1000 примерах)
- Генерация ответов: ~5-10 минут
- **Всего: ~1-1.5 часа**

## 💡 Ускорение:

Для быстрого теста отредактируйте в `main.py` строку:
```python
max_samples=100  # вместо 1000
```

## ⚠️ Важно:

- Проверьте наличие токенов в `.env`
- Требуется ~10GB свободного места
- На macOS может работать медленнее из-за отсутствия CUDA

## 📖 Полная документация:

См. **INSTRUCTIONS.md** для детальных инструкций.
